AUTOPAS Library
Version 0.0.1 prealpha

==========BUILD============
build instructions:

mkdir build
cd build
cmake ..
make

==========TESTS============
to run tests:

make test

or using the ctest environment:

ctest

to get verbose output:

ctest --verbose
